---
import BlogTemplate from '../../components/templates/BlogTemplate.astro';
import { getLocaleData } from '../../utils/i18n';

// Get blog data from Spanish locale
const blog_data = await getLocaleData({ language: 'es', page: 'blog' });
---

<BlogTemplate blog_data={blog_data} lang="es" />
