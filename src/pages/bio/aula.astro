---
import { Image } from "astro:assets";
import Layout from "@components/Layout.astro";
import aulaImg from "@components/bio-items/aula-f75/F75.avif";
---

<Layout
  lang="en"
  title="AULA F75 — Javier Miz"
  description="Teclado Mecánico 75% Inalámbrico"
  hide_header={true}
  hide_footer={true}
  disable_main_padding={true}
>
  <div class="landing">
    <div class="bg-glow"></div>

    <a href="/bio" class="back">← Volver</a>

    <div class="content">
      <div class="hero">
        <Image src={aulaImg} alt="AULA F75" width={600} height={600} class="img" />
      </div>

      <div class="info">
        <span class="badge">Gasket Mount · Tri-Mode</span>
        <h1 class="name">AULA F75</h1>
        <p class="tagline">Teclado Mecánico 75% · Sonido Premium</p>
      </div>

      <div class="cta-group">
        <a class="cta-primary" href="https://s.click.aliexpress.com/e/_c4mdq9mV" target="_blank" rel="noreferrer">
          <span>🛒</span>
          <span>Comprar en AliExpress</span>
        </a>
      </div>
    </div>
  </div>
</Layout>

<style>
  :global(body) {
    background: #080808 !important;
    margin: 0;
    padding: 0;
  }

  .landing {
    position: relative;
    width: 100%;
    height: 100vh;
    height: 100dvh;
    background: #080808;
    color: #fff;
    font-family: var(--font-serif);
    display: flex;
    flex-direction: column;
    overflow: hidden;
  }

  .bg-glow {
    position: absolute;
    top: -30%;
    left: 50%;
    transform: translateX(-50%);
    width: 600px;
    height: 600px;
    background: radial-gradient(circle, rgba(0, 212, 170, 0.1) 0%, transparent 60%);
    pointer-events: none;
  }

  .back {
    position: absolute;
    top: 1rem;
    left: 1.25rem;
    z-index: 2;
    font-size: 0.75rem;
    color: #555;
    text-decoration: none;
    min-height: 44px;
    line-height: 44px;
    transition: color 0.2s;
  }

  .back:hover { color: #00D4AA; }

  .content {
    position: relative;
    z-index: 1;
    flex: 1;
    display: flex;
    flex-direction: column;
    align-items: center;
    justify-content: center;
    padding: 3rem 1.5rem 2rem;
    gap: 0;
  }

  .hero {
    flex: 1;
    display: flex;
    align-items: center;
    justify-content: center;
    min-height: 0;
  }

  .img {
    max-width: 300px;
    max-height: 38vh;
    width: auto;
    height: auto;
    object-fit: contain;
    filter: drop-shadow(0 10px 30px rgba(0, 212, 170, 0.12));
  }

  .info {
    text-align: center;
    margin-bottom: 1rem;
  }

  .badge {
    display: inline-block;
    font-size: 0.55rem;
    font-weight: 600;
    letter-spacing: 0.1em;
    text-transform: uppercase;
    color: #00D4AA;
    background: rgba(0, 212, 170, 0.08);
    border: 1px solid rgba(0, 212, 170, 0.2);
    padding: 0.2rem 0.6rem;
    border-radius: 100px;
    margin-bottom: 0.5rem;
  }

  .name {
    font-size: 1.4rem;
    font-weight: 800;
    line-height: 1.1;
    margin: 0 0 0.25rem;
    background: linear-gradient(135deg, #00D4AA 0%, #00FFCC 100%);
    -webkit-background-clip: text;
    -webkit-text-fill-color: transparent;
    background-clip: text;
  }

  .tagline {
    font-size: 0.7rem;
    color: #666;
    margin: 0;
  }

  .cta-group {
    width: 100%;
    max-width: 360px;
  }

  .cta-primary {
    display: flex;
    align-items: center;
    justify-content: center;
    gap: 0.5rem;
    width: 100%;
    min-height: 52px;
    background: linear-gradient(135deg, #00D4AA 0%, #00FFCC 100%);
    color: #080808;
    font-size: 0.85rem;
    font-weight: 700;
    text-decoration: none;
    border-radius: 12px;
    box-shadow: 0 4px 20px rgba(0, 212, 170, 0.3);
    transition: transform 0.15s, box-shadow 0.15s;
  }

  .cta-primary:hover {
    transform: translateY(-2px);
    box-shadow: 0 8px 30px rgba(0, 212, 170, 0.4);
  }

  @media (min-width: 768px) {
    .img { max-width: 400px; max-height: 42vh; }
    .name { font-size: 2rem; }
    .tagline { font-size: 0.85rem; }
    .cta-primary { font-size: 0.95rem; }
  }
</style>
