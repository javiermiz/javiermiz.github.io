---
import BlogTemplate from "../components/templates/BlogTemplate.astro";
import { getLocaleData } from "../utils/i18n";

// Get blog data from locale
const blog_data = await getLocaleData({ language: "en", page: "blog" });
---

<BlogTemplate blog_data={blog_data} lang="en" />
