---
export interface Props {
  name: string;
  level?: string;
  isLast?: boolean;
}

const { name, level, isLast = false } = Astro.props;
---

<span>
  <span>
    {name}
  </span>
  {level && (
    <span>
      {level}
    </span>
  )}
</span>
{!isLast && (
  <span>•</span>
)}